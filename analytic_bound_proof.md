# M_{k,ε} < 4 的严格解析排除（纯有理证书）

日期：2026-08-22。所有断言不依赖浮点运算；数值部分（ln k 上界）为纯有理数
幂级数证书。脚本：`dsh_scan/analytic_bound_cert.py`（输出 `analytic_cert_output.txt`）。

## 1. 定理陈述

**定理 A**：设 `k ≥ 2`，`0 ≤ ε < 1`。若

```
(1+ε) · k/(k−1) · ln k  <  4 ,                       (★)
```

则 `M_{k,ε} < 4`（Maynard–Polymath8b 筛法泛函的严格上界）。

**证明**：
1. Polymath8b（arXiv:1407.4897，Remark 6.6）：对任意 `a` 满足
   `1/(1+ε) < a < 1/(1−ε)` 有

   ```
   M_{k,ε} ≤  k/(a(k−1)) · ln( k + (a(1+ε)−1)(k−1) / (1−a(1−ε)) ) .   (R)
   ```

   （该式由带权重 `1 + a(−t₁−⋯−t_k + kt_i)` 的 Cauchy–Schwarz 论证得到；
   a=1 时即 Proposition 6.5 的 `M_{k,ε} ≤ k/(k−1)·ln(2k−1)`。）
2. 令 `F(a)` 记 (R) 右端。直接代入 `a = 1/(1+ε)` 得：
   `a(1+ε) = 1`、`1−a(1−ε) = 2ε/(1+ε)`，故
   `F(1/(1+ε)) = (1+ε)·k/(k−1)·ln k`。`F` 在 `a₀ := 1/(1+ε)` 的右邻域连续
   （对 ε>0，分母 `1−a(1−ε) → 2ε/(1+ε) > 0`；对 ε=0 用 Lemma 6.4 单独处理）。
3. 由 (★) 与连续性，存在 `δ>0` 使 `F(a₀+δ) < 4`，且 `a₀+δ < 1/(1−ε)`。
   取 `a = a₀+δ` 代入 (R) 得 `M_{k,ε} ≤ F(a) < 4`。∎

**定理 B（ε=0）**（Lemma 6.4）：`M_k ≤ k/(k−1)·ln k`。故对 k ≤ 50 有
`M_k ≤ 3.9918602… < 4`（k=50 最大）。

## 2. 纯有理证书（ln k 上界）

`ln k = 2·artanh((k−1)/(k+1))`，`artanh(z) = Σ_{n≥0} z^{2n+1}/(2n+1)`（|z|<1 正项级数）。
截断到 n=200：余项

```
R = Σ_{n>200} z^{2n+1}/(2n+1) ≤ z^{403}/(403·(1−z²)) ,
```

因为 `2n+1 > 403` 时 `1/(2n+1) < 1/403` 且 `Σ_{n>200} z^{2n+1} = z^{403}/(1−z²)`。
全部用 Python `Fraction` 精确比较 `(1+ε)·k/(k−1)·(2·(Σ+R)) < 4`——
零浮点、零区间信任边界。`z = (k−1)/(k+1) < 1`，级数收敛极快（z ≤ 49/51）。

## 3. 结果表（有理证书，逐 (k,ε) 布尔判定）

| k | ε=0 | ε=1/200 | ε=1/100 | ε=1/50 | ε=1/25 |
|---|---|---|---|---|---|
| 40–42 | STRICT<4 | STRICT<4 | STRICT<4 | STRICT<4 | STRICT<4 |
| 43–46 | STRICT<4 | STRICT<4 | STRICT<4 | STRICT<4 | upper>4 |
| 47–48 | STRICT<4 | STRICT<4 | STRICT<4 | upper>4 | upper>4 |
| 49 | STRICT<4 | STRICT<4 | upper>4 | upper>4 | upper>4 |
| 50 | STRICT<4 | upper>4 | upper>4 | upper>4 | upper>4 |

（"upper>4" = 该 (k,ε) 下 (★) 不成立，解析方法不排除——**不代表 M ≥ 4**。）

**连续 ε 排除区间**（(★) 对 ε 单调 ⟹ 闭区间成立）：

| k | 严格排除区 | 说明 |
|---|---|---|
| 2–46 | 0 ≤ ε ≤ 1/50 | 含 Polymath8b 未做数值的 k=44–46 |
| 47 | 0 ≤ ε ≤ 1/60 | |
| 48 | 0 ≤ ε ≤ 1/86 | |
| 49 | **0 ≤ ε ≤ 1/147** | ε=0 的 3.9729 特例推广为连续区间 |
| 50 | 0 ≤ ε ≤ 1/491 | Polymath8b 用 ε=1/25 ≫ 1/491（一致） |
| ≥51 | 无（ε=0 上界已 >4） | (51/50)·ln51 = 4.0105 > 4 |

## 4. 意义与本项目数值范围的对照

1. 本项目 ε=0 的混合基扫描（k=44..50，M=3.804..3.907，旧值）与新高精度值
   （k=49: 3.9106）：全部落在定理 B 的严格排除区内，**无需任何数值计算**。
2. 本项目 ε=1/50 的混合基扫描（k=44..50）：k ≤ 46 由定理 A 严格排除；
   k=47..50 的**子空间**严格判定（gmpy2 精确 LDL，`strict_eps50_k*.log`）进行中，
   高精度数值（legendre_fix）给出 3.9267/3.9441/3.9611/3.9776（全部 < 4）。
3. k=49 的"超越 246"可行窗口：ε ∈ [1/147, 1)。ε=1/50 与 ε=1/25 都在窗口内
   ——与 Polymath8b 将 k=49 列为开放问题的结论一致；本证书把窗口**下限**从
   0 提升到 1/147（此前仅知 ε=0 单点排除）。
4. k ≥ 51 不受影响：Polymath8b 的 M_{51,1/50} > 4.00156 在排除区外，一致。


## 6. 子空间严格判定（区间算术，iv_strict3.py，2026-08-23）

解析方法（定理 A/B）对 k=47..50 @ ε=1/50 不排除（上界 > 4）。对这些 k 的
**混合基子空间**（{(1-P1)^r p_γ}，D=20，n=707）给出严格 < 4 判定：

**方法**：A = (4/k)·I − J₁（Fraction 精确构建）→ mpmath iv 区间（400 dps，
单步宽度 ~1e-400）→ **对角平衡**：A' = D·A·D，D_ii = 1/√A_ii（区间），
正对角合同变换保持正定性（A' 正定 ⟺ A 正定），平衡后主元 ~1e-14 级
（原基主元 ~1e-83..1e-111，区间传播会爆炸——平衡是关键预处理）→
**对称选主元区间 LDL^T**（每步选最大剩余对角，行/列对称交换）。

**结论**：全部 707 主元区间下界 > 0 ⟹ A 正定 ⟹ λ_max(J₁,I) < 4/k ⟹
**M_{k,1/50} < 4 严格**。区间算术的包含性保证真值必在区间内；主元区间
下界 > 0 是数学上无懈可击的证明（iv 区间端点各按向下/向上舍入，覆盖全部
中间误差，无浮点信任边界）。

| k | 判定 | 最小主元量级 | 耗时 |
|---|---|---|---|
| 44 | STRICT M<4 | ~1e-14 | 1158s |
| 45 | STRICT M<4 | ~1e-14 | 1163s |
| 46 | STRICT M<4 | ~1e-14 | 1161s |
| 47 | STRICT M<4 | ~1e-14 | 1146s |
| 48 | STRICT M<4 | ~1e-14 | 1159s |
| 49 | STRICT M<4 | 6.6e-29 | 1147s |
| 50 | STRICT M<4 | ~1e-14 | 1137s |

日志：`dsh_scan/iv_strict3_k{44..50}.log`（k=49 为 `iv_strict3_k49_v2.log`）。
补充判定（2026-08-23 15:00 启动）：k=49 D=23（n=1236）ε=1/50 与
k=49 D=20 ε=1/25——`iv_strict3_k49_d23.log`、`iv_strict3_k49_eps25.log`。

**局限**：子空间 sup 是真实 M_{k,ε} 的**下界**——"子空间 < 4"不排除全空间
> 4。全空间（论文对称单项式基，n~2400-2500）的严格判定仍开放（精确 LDL
大整数爆炸；区间方法在 n~2500 需 400dps 区间 LDL ~10-20h/个，可作后续工作）。

## 5. 局限（诚实声明）

- 定理 A 是**充分条件**（(★)），不是 M_{k,ε} 的真值；k=47..50 @ ε=1/50 的
  M_{k,ε} 是否 < 4 仍是**开放问题**（解析上界 4.0125/4.0326/4.0524/4.0717 > 4）。
- "upper>4" 仅表示解析上界 > 4，**不构成** M ≥ 4 的证据。
- 证书脚本的数学内容依赖：(i) Polymath8b Remark 6.6 公式（已核对论文源文件
  newergap-submitted.tex）；(ii) artanh 幂级数余项界（初等，文中给出证明）。
