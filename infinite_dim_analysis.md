# 无限维全空间分析：M_{k,ε} 的严格上界（2026-08-26）

## 1. 问题与已解决部分

**开放问题**：对所有 L² 函数 F（支撑在 (1+ε)R_k），是否 M_{k,ε} < 4？
——即无限维变分问题 sup_F (ΣJ_i(F))/I(F) < 4。

**已严格解决（无限维）**：
- **定理 A**（纯有理证书，见 analytic_bound_proof.md）：
  - `2 ≤ k ≤ 46, 0 ≤ ε ≤ 1/50` ⟹ M_{k,ε} < 4（对所有 L² 函数）；
  - `k=49, ε ≤ 1/147` ⟹ M_{49,ε} < 4（无限维）；
  - `k=50, ε ≤ 1/491` ⟹ M_{50,ε} < 4（无限维）。
  证明：Polymath8b Remark 6.6 的 Cauchy-Schwarz 上界对**任意** F 成立
  （无限维），端点 a→1/(1+ε) 给出 M_{k,ε} ≤ (1+ε)(k/(k−1))·ln k。
- 有限维判定（论文基 D=25/27/29）给出**下界**方向（子空间 sup）。

## 2. CS 方法的无限维上界：结构分析

Lemma cs（Polymath8b）：对任意正可测 G_i（∫_0^∞ G_i dt_i ≤ 1）：

$$M_{k,\varepsilon} \le \sup_{t \in (1+\varepsilon)R_k} \sum_{i=1}^k \frac{1}{G_i(t)}$$

对称化（G_i(t) = g(s_i)，s_i = 1+ε−Σt+kt_i）后：
- 可达集：{s ≥ 0 : Σs_i = k(1+ε)}（全单纯形）；
- 约束：∫_{s0}^{ks0} g ≤ k−1 对所有 s0 ∈ (0, 1+ε]；
- 上界 = sup_{simplex} Σ 1/g(s_i)。

**论文选择** g(s) = C/s（C = (k−1)/ln k）⟹ 上界 = k(1+ε)·ln k/(k−1)
（ε=1/50, k=49: **4.0524**；ε=1/25: 4.1318）。

## 3. CS 方法无法改进的证据

1. **幂族** g = C·s^{-p}：解析证明 p=1 最优（上界 ≥ 论文值，p≠1 时更大）。
2. **线性族** g = 1/(as+b)：解析证明 b=0 最优（= 论文）。
3. **凹/凸幂族** g = (as+b)^p：数值优化全部 ≥ 论文值。
4. **随机采样**：60 次随机 φ(s)=1/g(s)（对数网格分段线性 + 平滑扰动），
   全部上界 ≥ 论文值（0 次低于）。
5. **双层优化**（cs_opt7/8：DE/Nelder-Mead 优化 φ 节点值，内层精确
   simplex sup）：k=47 收敛 4.275 > 4.0125；均未找到 < 论文的解。
6. **最终彻底优化**（cs_final.py：k=49，DE 长跑）：结果待收。

**逐点引理**（已证）：对任意 g 满足约束，存在 s ∈ [0, kS] 使
1/g(s) ≥ s·ln k——g 无法处处优于论文形状。

**猜想（CS 下界定理）**：对任意可行 g，
sup_{simplex} Σ1/g(s_i) ≥ k(1+ε)·ln k/(k−1)——即线性 φ（论文）是
CS 方法族的**全局最优**。若成立，则 **CS 方法（文献唯一的上界工具）在
ε=1/25、1/50 时无法证明 M_{49,ε} < 4**——上界 4.0524/4.1318 是该方法极限。

## 4. 诚实边界

- 无限维 L² 全空间的严格判定：**ε ∈ (1/147, 1) 时是数学开放问题**。
- 已解决：ε ≤ 1/147（定理 A，无限维严格）；论文基子空间（D≤29）
  **严格 < 4**（下界方向）。
- 若 CS 下界定理成立：证明"CS 方法不可达"——但不排除**其他方法**
  （如改进的权重结构、谱分析、数值验证 + 严格余项界）。
- **反方向**：证明 M_{49,ε} ≥ 4 需要无限维下界——我们的数值下界 3.99
  （D=29 论文基）不支撑（真值 ≥ 3.99，无法闭合到 ≥ 4）。

## 5. 工具与日志

- cs_upper.py / cs_opt.py / cs_opt2.py / cs_opt3.py / cs_opt4.py /
  cs_opt5.py / cs_opt7.py / cs_opt8.py / cs_final.py / f_eval.py /
  f_upper.py / eigenvec_extract.py——CS 上界分析工具链。
- 特征函数方法（G_i = F/∫F dt_i）：λ_max 特征向量提取成功
  （ev_k49_d20_eps1_50.npy），但 F 有负区域（特征函数不正），
  |F| 修正的上界爆炸（37.7）——该路线不可行。

## 6. 收敛率分析与对称化（2026-08-26 补充，D=31 高精度点 2026-08-29 03:12 更新）

**对称化引理**（Polymath8b 论文 §8）：M_k 的 sup 无损失地限制在对称函数上
（F 的对称化 + 三角不等式）。ε 版本同样成立（(1+ε)R_k 对称）。论文基
（(1−P₁)^r·P_α，α 偶划分）在对称多项式（稠密于对称 L²）中成基 ⟹
**论文基有限维特征值的极限 = M_{k,ε} 真值**。

**收敛率（ε=1/25，D=19/23/25/27/29/31；D=31 为 legendre_fix 高精度 λ_max，
512 bits 精确 LDL^T 全空间正交化 + Rayleigh 有理证书）**：
| D | M | 增量 (per 2-D) |
|---|---|---|
| 19 | 3.9635 | — |
| 23 | 3.9766 | 0.0131 |
| 25 | 3.9843342389 | 0.00773 |
| 27 | 3.9870762843 | 0.00274 |
| 29 | 3.9887309107 | 0.00165 |
| 31 | **3.9897057566** | **0.000975** |

**拟合（5 严格点）**：增量比 0.6034 → 0.5892（单调递减，几何衰减 q ≈ 0.596）——
**外推极限 ≈ 3.9902 < 4（裕量 0.0098）**；Aitken Δ² = 3.9911；保守上界
（q→q_last）≈ 3.9902。其他会话独立外推（偶基非无损 δ 指数衰减）：真值
≈ 3.9936 < 4；D=33 偶基 3.990155 一致。

**结论（强证据，非严格证明）**：M_{49,1/25} 真值 ≈ 3.990-3.991 < 4。
**严格化路径**（后续研究）：(a) 加权 Jacobi 谱方法的特征值收敛率证明
（核的解析性 → 指数收敛 + 显式常数）；(b) CS 下界定理的完整证明。
两者均为论文级数学。

## 7. 移位特征函数方法——数值障碍诊断（2026-08-26 深夜）

**方法**：G_i = F'/∫F'（F' = F_N + δ·1，δ 使 F' 正）——上界 = sup Σ∫F'/F'。
若上界 < 4 ⟹ 无限维证明。

**诊断**：
1. **F 的病态缩放**：特征向量 v 的 float L2 归一化与 I 内积差 126 个数量级
   （v^T I v = 3.92e-126）——F 的"普通点值" ~1e-25（I 内积归一化后 ~1e38）——
   未缩放的 F 在 float64 下评估不稳定（相对误差大）——**早期残差检查的
   3.987 结果数值不可靠**。
2. **F 的符号结构**：D=27 特征向量系数半正半负（1260/1266）——F 在 (1+ε)R_k
   的采样点大部分 ≈ 0（bfi/gauss 验证 3 点全 F≈0）——F 是符号振荡函数——
   移位方法需要 δ 覆盖负部——上界可能差。
3. **评估效率**：D=27（2526 基）的 F 评估 ~3-10s/点——sup 优化不可行——
   bfi 解析积分（∫F dt₁ 的 Beta 公式）原型已写（bfi_int.py）——验证中——
   完整实现（DP 一次输出所有 subsets）是后续关键。
4. **采样稳定性**：signal.alarm 与 numpy 交互导致进程挂起——需用线程/分块。

**结论**：移位方法受 F 的符号振荡与数值缩放阻碍——严格化需 bfi 全实现 +
正确缩放的 F + 全局 sup 优化（论文级工程）。**收敛率路径（几何衰减
0.696^D，外推 3.9939）仍是主要严格化方向**——D=31 严格判定运行中（~08:00）。
