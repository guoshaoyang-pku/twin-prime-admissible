# Feature: Polymath8b 上界证明缺陷（Mkeps / Remark 6.6）

**状态**: 已核验（verify_mkeps_gap.py 全部 5 项通过，精确有理数 + mpmath 50dps）
**论文**: D.H.J. Polymath, "New equidistribution estimates of Zhang type",
arXiv:1407.4897 — **Proposition 6.5**（M_{k,ε} ≤ k/(k−1)·log(2k−1)）
与 **Remark 6.6**（a 参数化族）。

---

## 1. 缺陷陈述（一句话）

在 a > 1/(1+ε) 的范围内（含 a=1 的 Prop 6.5），论文上界的证明链
**不严格**：其"求和步骤"需要 Σ_{有效切片} w_i(t) ≤ k，但存在反例点
t = (1+ε, 0, ..., 0) 使该量为 k(1+ε)−ε > k（对 ε > 1/(k−1)）。

## 2. 记号与论文证明结构

对 k ≥ 2, 0 ≤ ε < 1，M_{k,ε} = sup_F [Σ_i J_{i,1−ε}(F)]/I(F)，其中
- F 支撑在 (1+ε)R_k（R_k = {t ≥ 0 : Σt ≤ 1}）；
- J_{i,1−ε}(F) = ∫_{(1−ε)R_{k−1}} (∫₀^∞ F dt_i)² dt_{≠i}；
- I(F) = ∫_{(1+ε)R_k} F² dt。

论文 Prop 6.5 的证明（tex 行 1965–1982）：对 t_{≠i} ∈ (1−ε)R_{k−1}
（记 s := 1−Σ_{j≠i}t_j ≥ ε）：
∫₀^{s+ε} dt_i/(s+(k−1)t_i) = (1/(k−1))·log((ks+(k−1)ε)/s) ≤ (1/(k−1))·log(2k−1)
⟹ 由 Cauchy–Schwarz：(∫F dt_i)² ≤ (log(2k−1)/(k−1))·∫(1−Σt+kt_i)F² dt_i。
**"Integrating in t_{≠i} and summing in i, we obtain the claim."**

## 3. 缺陷：求和步骤的隐藏假设

记 w_i(t) := 1−Σt+kt_i = s_i+(k−1)t_i（s_i = 1−Σ_{j≠i}t_j）。最后一步需要

  Σ_i ∫_{D_i} w_i(t) F(t)² dt ≤ k·I(F),  D_i := {t : t_{≠i} ∈ (1−ε)R_{k−1}}.   (∗)

全空间恒等 Σ_{i=1}^k w_i(t) = k 只在**对所有 i 求和**时成立（含无效切片的
负项抵消）。对**有效切片** E(t) := {i : t_{≠i} ∈ (1−ε)R_{k−1}} 求和：

**反例（k=49, ε=1/25, t₀ = (1+ε, 0, ..., 0)）**：
- u := Σt = 1+ε，仅 i=1 有效（t_{≠1} = 0 ∈ (1−ε)R_{k−1}；其余 t_{≠i} 含
  t₁ = 1+ε > 1−ε）；
- **Σ_{i∈E(t₀)} w_i(t₀) = w₁ = 1−(1+ε)+k(1+ε) = k(1+ε)−ε = 1273/25 = 50.92**；
- **50.92 > k = 49** ⟹ (∗) 不成立；
- 该点的论文 CS 上界值 = log(2k−1)/(k−1)·50.92 = **4.853 > 4.67**
  （声称界 k/(k−1)·log(2k−1)）——证明的中间量被逐点违反。

**缺陷条件**：k(1+ε)−ε > k ⟺ (k−1)ε > 0（恒真）？——不：w₁ = k(1+ε)−ε，
需要 w₁ > k ⟺ k+kε−ε > k ⟺ ε(k−1) > 0 ⟺ ε > 0。**对任何 ε > 0 都违反**
（不只是 ε > 1/(k−1)）！数值：ε=1/25 时 50.92 > 49；ε→0⁺ 时 w₁ = k+ε(k−1) → k⁺。
（注：ε=0 时 t₀ = (1,0,...,0)，w₁ = 1−1+k = k，等式成立——缺陷只出现在 ε > 0。）

## 4. Remark 6.6 的 a 参数化：严格界限

w_i^a(t) := 1+a(−Σt+kt_i)。对有效集 E（|E| = m）：
Σ_{i∈E} w_i^a = m(1−au) + ak·Σ_{i∈E}t_i ≤ m(1−au) + aku = m + au(k−m),
其中 u = Σt ≤ 1+ε。

- **a ≤ 1/(1+ε)**：au ≤ 1 ⟹ Σ_E w_i^a ≤ m + (k−m) = k ✓ **严格**；
- **a > 1/(1+ε)**：反例 t₀ = (1+ε, 0, ..., 0)（m=1, u=1+ε）给出
  Σ_E w_i^a = 1 + a(1+ε)(k−1) > k ✗ **不严格**。

**推论**：Remark 6.6 的 a 参数化在 a ∈ (1/(1+ε), 1/(1−ε))（论文声称的
范围）内证明不严格；**最优严格值 = a → 1/(1+ε) 端点**：
F(1/(1+ε)) = k/(a(k−1))·log(k + 0) = **(1+ε)·k/(k−1)·log k = 4.131816…**
（k=49, ε=1/25）——**正是本项目定理 A 的 (★)**。

## 5. 数值核验（verify_mkeps_gap.py，全部通过）

| # | 核验项 | 结果 |
|---|---|---|
| 1 | 反例点 Σ_{有效}w_i = k(1+ε)−ε = 1273/25 = 50.92 > k = 49 | ✓ 精确有理 |
| 2 | 论文 CS 上界在反例点 = 4.853 > 声称界 4.670 | ✓ |
| 3 | a ≤ 1/(1+ε)：max_t Σ_E w_i^a ≤ k（解析 + 400 网格数值 = 49.000） | ✓ |
| 4 | a > 1/(1+ε)：反例 Σ_E w_i^a > k | ✓ |
| 5 | F(1/(1+ε)) = 4.13181588316 = (1+ε)k/(k−1)·log k | ✓ mpmath 50dps |

## 6. 含义与诚实边界

1. **文献修正**：Prop 6.5（4.67）与 Remark 6.6（a > 1/(1+ε)）的证明在
   ε > 0 时不严格——这些上界**数值上仍真**（M_{49,1/25} ≈ 3.99 远小于
   4.67/4.13），但**证明链断裂**，不能作为严格引用。
2. **我们的定理 A 是最优严格 CS 上界**：该形状族（逐 i 线性权）的严格
   极限恰为 (1+ε)k/(k−1)·log k。
3. **不涉及孪生素数猜想本体**：不改变 246 的任何结果（论文的 k=50
   下界 4.0024 > 4 是数值下界，不受上界证明影响）。
4. **核验限制**：反例点是单点构造（精确）；数值扫描（第 3 项）为辅助
   交叉确认，主证是解析不等式 m + au(k−m) ≤ k。

## 7. 复现

```
python3 verify_mkeps_gap.py        # 全部断言通过即核验成功
```

依赖：Python 3.10+，mpmath（第 5 项 50dps 高精度）。
无浮点信任边界：第 1 项为精确 Fraction，第 5 项为 mpmath 50dps。
