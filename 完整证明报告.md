# 有界素数间隔：完整证明报告（最终版）

## 第一部分：可容许 k 元组最小直径（本会话严格证明）

**定义**：k 元整数集 T 可容许 ⟺ 对每个素数 p ≤ k，T 模 p 的剩余类未覆盖全部 p 类。
H(k) = 最小直径。可容许性对平移不变。

### 已闭环的定理（SAT 见证 + 全部 d < H(k) 的 UNSAT 穷举证明，自足）
| 定理 | SAT 侧 | UNSAT 侧 |
|---|---|---|
| **H(43) = 200** | 显式见证 + 独立验证 + Lean 4 | d < 200 全部 UNSAT ✓ |
| **H(44) = 210** | 显式见证 + 独立验证 + Lean 4 | d ≤ 208 全部 UNSAT ✓ |
| **H(46) = 216** | 14 个显式见证 + 独立验证 + Lean 4 | d < 216 全部 UNSAT ✓ |
| H(45) = 212 | 显式见证 + Lean 4 | d ≤ 208 UNSAT ✓（d=210 计算中） |
| H(50) = 246 | 显式见证 + 独立验证 + Lean 4 | d ≤ 226 UNSAT ✓（d=228..244 计算中） |

### 严格证明方法
- **SAT**：显式元组 + 逐素数剩余类检查（独立 Python 验证器 + Lean 4 机器验证）
- **UNSAT**：带频数剪枝的完备 DFS——对每个偶数 d < H(k)，搜索空间穷尽（无遗漏），
  证明不存在可容许 k 元组。每个 UNSAT 判定是精确计算（整数算术，无浮点）。
- **交叉验证**：与 OEIS A008407（Clark–Jarvis 2001）逐项一致（43 项零失配）

## 第二部分：M 泛函严格结果（Maynard 筛法判据）

**判据**（Polymath8b，无条件，Bombieri–Vinogradov）：M_{k,ε} > 4 ⇒ DHL[k,2] ⇒ 素数间隔 ≤ H(k)。

### 族内精确值（有理数二分，严格区间，宽度 < 1e-10）
| k | D=8 族内 M | 判定 |
|---|---|---|
| 46 | **3.6518799046** | 族内 < 4（严格） |
| 49 | **3.6970516836** | 族内 < 4（严格） |
| 50 | **3.7111477763** | 族内 < 4（严格） |

### 严格判定方法
- 幂和多项式基（度数 ≤ D）× (1+ε−Σt)^r 截断
- 全部积分 = 精确有理数闭式（Dirichlet 恒等式，独立推导并逐项验证）
- LDL^T 惯性定理：λ_max(J1, I) 与 4/k 的大小关系由精确有理数 LDL^T 分解的枢轴符号严格判定
- **零近似**：全程整数/有理数算术

### 解析上界（论文 Proposition，全空间）
M_{k,ε} ≤ k/(a(k−1))·log(k + (a(1+ε)−1)(k−1)/(1−a(1−ε)))
- **M_{44,1/50} < 3.949**（完整证明）
- **M_{46,1/50} < 3.991**（完整证明）

## 第三部分：Lean 4 形式化
TwinPrimeAdmissible.lean：**89 定理，EXIT 0，零 sorry/axiom**（仅 import Init）
- 六关键见证（k=43..50）可容许性 + 直径
- k=46 的 14 个下行见证
- H(2)..H(12) 精确最小性（穷举机器证明）
- 结构定理：平移不变性、任意 k 存在可容许元组、单调性

## 第四部分：超越 246 的评估（诚实结论）

**元组侧**：H(46)=216 < H(50)=246——46 元组可以比纪录元组更窄（已严格证明）。

**筛法侧**：判据需要 M_{k,ε} > 4。
- 数值与理论证据（渐近标定 + 族内收敛）一致表明真实 M_{k<50,ε} < 4
- 严格结果：族内（D=8）M < 4（k=46/49/50）；解析上界对 (44, 1/50)、(46, 1/50) < 4
- **未闭合**：对全部 (k<50, ε) 证明 M < 4 需要比论文解析上界更紧的界
  （k=46, ε=1/25 处缺口 ~1.75%）——这是研究级问题

**结论**：本会话交付了元组侧的完整自足证明（H(43/44/46) 闭环，H(45)/H(50) 收尾中）
与筛法侧的严格工具链与部分严格结果。超越 246 的最终判定需要解决上述解析缺口。
